<Window x:Class="ParkingGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ParkingGUI"
        mc:Ignorable="d" FontSize="20"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Label Content="Nummerplade: " VerticalAlignment="Center" Grid.Row="1" Grid.Column="1"/>
        <TextBox x:Name="textBoxLicenseNumber" VerticalAlignment="Center"
                 Grid.Row="1"
                 Grid.Column="2" />
        <Button Content="Check" 
                Margin="10"
                Grid.Row="1" 
                Grid.Column="3" 
                Height="50"
                Click="Button_Click" />
    </Grid>
</Window>
